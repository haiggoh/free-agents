{
  "_comment": "Sky-blue theme for LOCAL MLX sessions. Applied via per-session settings overlay — never written to user's persistent ~/.claude/settings.json.",
  "_contract": "Theme accent color #87CEEB (sky blue). Provides session-scoped identity markers for launcher to inject via --settings.",
  "theme": {
    "name": "local-sky",
    "accent_color": "#87CEEB",
    "session_emoji": "🦾",
    "identity_label": "Local inference session",
    "supported_surfaces": [
      "session_title",
      "startup_announcement",
      "statusline"
    ],
    "unsupported_surfaces": [
      "native_ascii_artwork",
      "managed_announcements"
    ]
  },
  "fallback": {
    "theme_identifier": "unknown",
    "session_emoji": "❓",
    "identity_label": "Unknown session"
  }
}